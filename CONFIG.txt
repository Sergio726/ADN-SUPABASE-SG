==========================================
CONFIGURACIÓN LOCAL — Alambres del Norte
==========================================

Punto de entrada del repo: docs/HANDOFF.md
Checklist de tareas:       docs/PENDIENTES.md

==========================================
INFORMACIÓN DE SUPABASE (no es secreta)
==========================================

Project URL:
https://vgsnfshbirzucddwbdpt.supabase.co

Panel:
https://supabase.com/dashboard/project/vgsnfshbirzucddwbdpt

Keys (anon + service role): Settings → API del panel.
NUNCA las pegues en este archivo ni en git.

==========================================
PASO 1: CREAR ARCHIVO .env.local
==========================================

OPCIÓN A - COPIAR ARCHIVO:
--------------------------
Copiá "env.local.example" a ".env.local" y completá las keys
desde el panel de Supabase.

OPCIÓN B - CREAR MANUALMENTE:
-----------------------------
Creá ".env.local" en la raíz con:

NEXT_PUBLIC_SUPABASE_URL=https://vgsnfshbirzucddwbdpt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu_anon_key_aqui
SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key_aqui
NEXT_PUBLIC_SITE_URL=http://localhost:3000

==========================================
PASO 2: BASE DE DATOS
==========================================

En un proyecto YA existente no re-apliques dumps históricos.

Migraciones vigentes: supabase/migrations/
Históricas:           supabase/migrations/archived/

Si levantás un entorno nuevo, mirá docs/HANDOFF.md.

==========================================
PASO 3 (OPCIONAL): DATOS DE PRUEBA
==========================================

SQL Editor → pegar supabase/seed.sql → Run

Esto crea proveedores, artículos y precios de ejemplo.

==========================================
PASO 4: INSTALAR Y EJECUTAR
==========================================

npm install
npm run dev

Abrir: http://localhost:3000

Antes de push: npm run build

==========================================
GOOGLE OAUTH (OPCIONAL)
==========================================

1. Google Cloud Console → OAuth 2.0
2. Authorized redirect URI:
   https://vgsnfshbirzucddwbdpt.supabase.co/auth/v1/callback
3. Supabase → Authentication → Providers → Google

==========================================
SI ALGO FALLA
==========================================

1. .env.local en la raíz, con keys del panel (no del example)
2. Consola del navegador (F12)
3. Logs de la terminal
4. docs/HANDOFF.md

==========================================
